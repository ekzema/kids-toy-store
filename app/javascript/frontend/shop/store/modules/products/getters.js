const getters = {
    products: state => state.products,
    product: state => state.product,
    wishListCounter: state => state.wishListCounter,
    productsAutocomplete: state => state.productsAutocomplete
}

export default getters
